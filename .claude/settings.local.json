{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "deno run -A .claude/hooks/deno_quality.ts"
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "WebSearch",
      "WebFetch(domain:github.com)",
      "Bash(deno fmt*)",
      "Bash(deno lint --fix*)",
      "Bash(deno check*)",
      "Bash(deno task*)",
      "Bash(func host start)"
    ]
  }
}
